What are various ways to add images into our App?Explain 
=>import images and add as src in <img>tag


What would happen if we do console.log(useState())?
=>It returns an array having two elements.first elem is a variable,second is an function



How will useEffect behave if we don't add a dependency array
=> The useEffect will be called after every render.whenever the component gets re-render useEffect will be called.


What is SPA
=>Single Page Application


What is difference between client side Routing and server side Routing?
=>server side Routing:here the css,js,html files are coming from the server then they are loaded on browser

client side Routing:here the component is itself coming from the app
